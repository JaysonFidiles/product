/* eslint max-len: 0 */

import {
  eat,
  lookaheadType,
  lookaheadTypeAndKeyword,
  match,
  next,
  popTypeContext,
  pushTypeContext,

} from "../tokenizer/index";
import {ContextualKeyword} from "../tokenizer/keywords";
import {TokenType, TokenType as tt} from "../tokenizer/types";
import {input, state} from "../traverser/base";
import {
  baseParseMaybeAssign,
  baseParseSubscript,
  baseParseSubscripts,
  parseArrow,
  parseArrowExpression,
  parseCallExpressionArguments,
  parseExprAtom,
  parseExpression,
  parseFunctionBody,
  parseIdentifier,
  parseLiteral,

} from "../traverser/expression";
import {
  baseParseExportStar,
  parseExport,
  parseExportFrom,
  parseExportSpecifiers,
  parseFunctionParams,
  parseImport,
  parseStatement,
} from "../traverser/statement";
import {
  canInsertSemicolon,
  eatContextual,
  expect,
  expectContextual,
  isContextual,
  isLookaheadContextual,
  semicolon,
  unexpected,
} from "../traverser/util";

function isMaybeDefaultImport(lookahead) {
  return (
    (lookahead.type === tt.name || !!(lookahead.type & TokenType.IS_KEYWORD)) &&
    lookahead.contextualKeyword !== ContextualKeyword._from
  );
}

function flowParseTypeInitialiser(tok) {
  const oldIsType = pushTypeContext(0);
  expect(tok || tt.colon);
  flowParseType();
  popTypeContext(oldIsType);
}

function flowParsePredicate() {
  expect(tt.modulo);
  expectContextual(ContextualKeyword._checks);
  if (eat(tt.parenL)) {
    parseExpression();
    expect(tt.parenR);
  }
}

function flowParseTypeAndPredicateInitialiser() {
  const oldIsType = pushTypeContext(0);
  expect(tt.colon);
  if (match(tt.modulo)) {
    flowParsePredicate();
  } else {
    flowParseType();
    if (match(tt.modulo)) {
      flowParsePredicate();
    }
  }
  popTypeContext(oldIsType);
}

function flowParseDeclareClass() {
  next();
  flowParseInterfaceish(/* isClass */ true);
}

function flowParseDeclareFunction() {
  next();
  parseIdentifier();

  if (match(tt.lessThan)) {
    flowParseTypeParameterDeclaration();
  }

  expect(tt.parenL);
  flowParseFunctionTypeParams();
  expect(tt.parenR);

  flowParseTypeAndPredicateInitialiser();

  semicolon();
}

function flowParseDeclare() {
  if (match(tt._class)) {
    flowParseDeclareClass();
  } else if (match(tt._function)) {
    flowParseDeclareFunction();
  } else if (match(tt._var)) {
    flowParseDeclareVariable();
  } else if (eatContextual(ContextualKeyword._module)) {
    if (eat(tt.dot)) {
      flowParseDeclareModuleExports();
    } else {
      flowParseDeclareModule();
    }
  } else if (isContextual(ContextualKeyword._type)) {
    flowParseDeclareTypeAlias();
  } else if (isContextual(ContextualKeyword._opaque)) {
    flowParseDeclareOpaqueType();
  } else if (isContextual(ContextualKeyword._interface)) {
    flowParseDeclareInterface();
  } else if (match(tt._export)) {
    flowParseDeclareExportDeclaration();
  } else {
    unexpected();
  }
}

function flowParseDeclareVariable() {
  next();
  flowParseTypeAnnotatableIdentifier();
  semicolon();
}

function flowParseDeclareModule() {
  if (match(tt.string)) {
    parseExprAtom();
  } else {
    parseIdentifier();
  }

  expect(tt.braceL);
  while (!match(tt.braceR) && !state.error) {
    if (match(tt._import)) {
      next();
      parseImport();
    } else {
      unexpected();
    }
  }
  expect(tt.braceR);
}

function flowParseDeclareExportDeclaration() {
  expect(tt._export);

  if (eat(tt._default)) {
    if (match(tt._function) || match(tt._class)) {
      // declare export default class ...
      // declare export default function ...
      flowParseDeclare();
    } else {
      // declare export default [type];
      flowParseType();
      semicolon();
    }
  } else if (
    match(tt._var) || // declare export var ...
    match(tt._function) || // declare export function ...
    match(tt._class) || // declare export class ...
    isContextual(ContextualKeyword._opaque) // declare export opaque ..
  ) {
    flowParseDeclare();
  } else if (
    match(tt.star) || // declare export * from ''
    match(tt.braceL) || // declare export {} ...
    isContextual(ContextualKeyword._interface) || // declare export interface ...
    isContextual(ContextualKeyword._type) || // declare export type ...
    isContextual(ContextualKeyword._opaque) // declare export opaque type ...
  ) {
    parseExport();
  } else {
    unexpected();
  }
}

function flowParseDeclareModuleExports() {
  expectContextual(ContextualKeyword._exports);
  flowParseTypeAnnotation();
  semicolon();
}

function flowParseDeclareTypeAlias() {
  next();
  flowParseTypeAlias();
}

function flowParseDeclareOpaqueType() {
  next();
  flowParseOpaqueType(true);
}

function flowParseDeclareInterface() {
  next();
  flowParseInterfaceish();
}

// Interfaces

function flowParseInterfaceish(isClass = false) {
  flowParseRestrictedIdentifier();

  if (match(tt.lessThan)) {
    flowParseTypeParameterDeclaration();
  }

  if (eat(tt._extends)) {
    do {
      flowParseInterfaceExtends();
    } while (!isClass && eat(tt.comma));
  }

  if (isContextual(ContextualKeyword._mixins)) {
    next();
    do {
      flowParseInterfaceExtends();
    } while (eat(tt.comma));
  }

  if (isContextual(ContextualKeyword._implements)) {
    next();
    do {
      flowParseInterfaceExtends();
    } while (eat(tt.comma));
  }

  flowParseObjectType(isClass, false, isClass);
}

function flowParseInterfaceExtends() {
  flowParseQualifiedTypeIdentifier(false);
  if (match(tt.lessThan)) {
    flowParseTypeParameterInstantiation();
  }
}

function flowParseInterface() {
  flowParseInterfaceish();
}

function flowParseRestrictedIdentifier() {
  parseIdentifier();
}

function flowParseTypeAlias() {
  flowParseRestrictedIdentifier();

  if (match(tt.lessThan)) {
    flowParseTypeParameterDeclaration();
  }

  flowParseTypeInitialiser(tt.eq);
  semicolon();
}

function flowParseOpaqueType(declare) {
  expectContextual(ContextualKeyword._type);
  flowParseRestrictedIdentifier();

  if (match(tt.lessThan)) {
    flowParseTypeParameterDeclaration();
  }

  // Parse the supertype
  if (match(tt.colon)) {
    flowParseTypeInitialiser(tt.colon);
  }

  if (!declare) {
    flowParseTypeInitialiser(tt.eq);
  }
  semicolon();
}

function flowParseTypeParameter() {
  flowParseVariance();
  flowParseTypeAnnotatableIdentifier();

  if (eat(tt.eq)) {
    flowParseType();
  }
}

export function flowParseTypeParameterDeclaration() {
  const oldIsType = pushTypeContext(0);
  // istanbul ignore else: this condition is already checked at all call sites
  if (match(tt.lessThan) || match(tt.typeParameterStart)) {
    next();
  } else {
    unexpected();
  }

  do {
    flowParseTypeParameter();
    if (!match(tt.greaterThan)) {
      expect(tt.comma);
    }
  } while (!match(tt.greaterThan) && !state.error);
  expect(tt.greaterThan);
  popTypeContext(oldIsType);
}

function flowParseTypeParameterInstantiation() {
  const oldIsType = pushTypeContext(0);
  expect(tt.lessThan);
  while (!match(tt.greaterThan) && !state.error) {
    flowParseType();
    if (!match(tt.greaterThan)) {
      expect(tt.comma);
    }
  }
  expect(tt.greaterThan);
  popTypeContext(oldIsType);
}

function flowParseInterfaceType() {
  expectContextual(ContextualKeyword._interface);
  if (eat(tt._extends)) {
    do {
      flowParseInterfaceExtends();
    } while (eat(tt.comma));
  }
  flowParseObjectType(false, false, false);
}

function flowParseObjectPropertyKey() {
  if (match(tt.num) || match(tt.string)) {
    parseExprAtom();
  } else {
    parseIdentifier();
  }
}

function flowParseObjectTypeIndexer() {
  // Note: bracketL has already been consumed
  if (lookaheadType() === tt.colon) {
    flowParseObjectPropertyKey();
    flowParseTypeInitialiser();
  } else {
    flowParseType();
  }
  expect(tt.bracketR);
  flowParseTypeInitialiser();
}

function flowParseObjectTypeInternalSlot() {
  // Note: both bracketL have already been consumed
  flowParseObjectPropertyKey();
  expect(tt.bracketR);
  expect(tt.bracketR);
  if (match(tt.lessThan) || match(tt.parenL)) {
    flowParseObjectTypeMethodish();
  } else {
    eat(tt.question);
    flowParseTypeInitialiser();
  }
}

function flowParseObjectTypeMethodish() {
  if (match(tt.lessThan)) {
    flowParseTypeParameterDeclaration();
  }

  expect(tt.parenL);
  while (!match(tt.parenR) && !match(tt.ellipsis) && !state.error) {
    flowParseFunctionTypeParam();
    if (!match(tt.parenR)) {
      expect(tt.comma);
    }
  }

  if (eat(tt.ellipsis)) {
    flowParseFunctionTypeParam();
  }
  expect(tt.parenR);
  flowParseTypeInitialiser();
}

function flowParseObjectTypeCallProperty() {
  flowParseObjectTypeMethodish();
}

function flowParseObjectType(allowStatic, allowExact, allowProto) {
  let endDelim;
  if (allowExact && match(tt.braceBarL)) {
    expect(tt.braceBarL);
    endDelim = tt.braceBarR;
  } else {
    expect(tt.braceL);
    endDelim = tt.braceR;
  }

  while (!match(endDelim) && !state.error) {
    if (allowProto && isContextual(ContextualKeyword._proto)) {
      const lookahead = lookaheadType();
      if (lookahead !== tt.colon && lookahead !== tt.question) {
        next();
        allowStatic = false;
      }
    }
    if (allowStatic && isContextual(ContextualKeyword._static)) {
      const lookahead = lookaheadType();
      if (lookahead !== tt.colon && lookahead !== tt.question) {
        next();
      }
    }

    flowParseVariance();

    if (eat(tt.bracketL)) {
      if (eat(tt.bracketL)) {
        flowParseObjectTypeInternalSlot();
      } else {
        flowParseObjectTypeIndexer();
      }
    } else if (match(tt.parenL) || match(tt.lessThan)) {
      flowParseObjectTypeCallProperty();
    } else {
      if (isContextual(ContextualKeyword._get) || isContextual(ContextualKeyword._set)) {
        const lookahead = lookaheadType();
        if (lookahead === tt.name || lookahead === tt.string || lookahead === tt.num) {
          next();
        }
      }

      flowParseObjectTypeProperty();
    }

    flowObjectTypeSemicolon();
  }

  expect(endDelim);
}

function flowParseObjectTypeProperty() {
  if (match(tt.ellipsis)) {
    expect(tt.ellipsis);
    if (!eat(tt.comma)) {
      eat(tt.semi);
    }
    // Explicit inexact object syntax.
    if (match(tt.braceR)) {
      return;
    }
    flowParseType();
  } else {
    flowParseObjectPropertyKey();
    if (match(tt.lessThan) || match(tt.parenL)) {
      // This is a method property
      flowParseObjectTypeMethodish();
    } else {
      eat(tt.question);
      flowParseTypeInitialiser();
    }
  }
}

function flowObjectTypeSemicolon() {
  if (!eat(tt.semi) && !eat(tt.comma) && !match(tt.braceR) && !match(tt.braceBarR)) {
    unexpected();
  }
}

function flowParseQualifiedTypeIdentifier(initialIdAlreadyParsed) {
  if (!initialIdAlreadyParsed) {
    parseIdentifier();
  }
  while (eat(tt.dot)) {
    parseIdentifier();
  }
}

function flowParseGenericType() {
  flowParseQualifiedTypeIdentifier(true);
  if (match(tt.lessThan)) {
    flowParseTypeParameterInstantiation();
  }
}

function flowParseTypeofType() {
  expect(tt._typeof);
  flowParsePrimaryType();
}

function flowParseTupleType() {
  expect(tt.bracketL);
  // We allow trailing commas
  while (state.pos < input.length && !match(tt.bracketR)) {
    flowParseType();
    if (match(tt.bracketR)) {
      break;
    }
    expect(tt.comma);
  }
  expect(tt.bracketR);
}

function flowParseFunctionTypeParam() {
  const lookahead = lookaheadType();
  if (lookahead === tt.colon || lookahead === tt.question) {
    parseIdentifier();
    eat(tt.question);
    flowParseTypeInitialiser();
  } else {
    flowParseType();
  }
}

function flowParseFunctionTypeParams() {
  while (!match(tt.parenR) && !match(tt.ellipsis) && !state.error) {
    flowParseFunctionTypeParam();
    if (!match(tt.parenR)) {
      expect(tt.comma);
    }
  }
  if (eat(tt.ellipsis)) {
    flowParseFunctionTypeParam();
  }
}

// The parsing of types roughly parallels the parsing of expressions, and
// primary types are kind of like primary expressions...they're the
// primitives with which other types are constructed.
function flowParsePrimaryType() {
  let isGroupedType = false;
  const oldNoAnonFunctionType = state.noAnonFunctionType;

  switch (state.type) {
    case tt.name: {
      if (isContextual(ContextualKeyword._interface)) {
        flowParseInterfaceType();
        return;
      }
      parseIdentifier();
      flowParseGenericType();
      return;
    }

    case tt.braceL:
      flowParseObjectType(false, false, false);
      return;

    case tt.braceBarL:
      flowParseObjectType(false, true, false);
      return;

    case tt.bracketL:
      flowParseTupleType();
      return;

    case tt.lessThan:
      flowParseTypeParameterDeclaration();
      expect(tt.parenL);
      flowParseFunctionTypeParams();
      expect(tt.parenR);
      expect(tt.arrow);
      flowParseType();
      return;

    case tt.parenL:
      next();

      // Check to see if this is actually a grouped type
      if (!match(tt.parenR) && !match(tt.ellipsis)) {
        if (match(tt.name)) {
          const token = lookaheadType();
          isGroupedType = token !== tt.question && token !== tt.colon;
        } else {
          isGroupedType = true;
        }
      }

      if (isGroupedType) {
        state.noAnonFunctionType = false;
        flowParseType();
        state.noAnonFunctionType = oldNoAnonFunctionType;

        // A `,` or a `) =>` means this is an anonymous function type
        if (
          state.noAnonFunctionType ||
          !(match(tt.comma) || (match(tt.parenR) && lookaheadType() === tt.arrow))
        ) {
          expect(tt.parenR);
          return;
        } else {
          // Eat a comma if there is one
          eat(tt.comma);
        }
      }

      flowParseFunctionTypeParams();

      expect(tt.parenR);
      expect(tt.arrow);
      flowParseType();
      return;

    case tt.minus:
      next();
      parseLiteral();
      return;

    case tt.string:
    case tt.num:
    case tt._true:
    case tt._false:
    case tt._null:
    case tt._this:
    case tt._void:
    case tt.star:
      next();
      return;

    default:
      if (state.type === tt._typeof) {
        flowParseTypeofType();
        return;
      } else if (state.type & TokenType.IS_KEYWORD) {
        next();
        state.tokens[state.tokens.length - 1].type = tt.name;
        return;
      }
  }

  unexpected();
}

function flowParsePostfixType() {
  flowParsePrimaryType();
  while (!canInsertSemicolon() && (match(tt.bracketL) || match(tt.questionDot))) {
    eat(tt.questionDot);
    expect(tt.bracketL);
    if (eat(tt.bracketR)) {
      // Array type
    } else {
      // Indexed access type
      flowParseType();
      expect(tt.bracketR);
    }
  }
}

function flowParsePrefixType() {
  if (eat(tt.question)) {
    flowParsePrefixType();
  } else {
    flowParsePostfixType();
  }
}

function flowParseAnonFunctionWithoutParens() {
  flowParsePrefixType();
  if (!state.noAnonFunctionType && eat(tt.arrow)) {
    flowParseType();
  }
}

function flowParseIntersectionType() {
  eat(tt.bitwiseAND);
  flowParseAnonFunctionWithoutParens();
  while (eat(tt.bitwiseAND)) {
    flowParseAnonFunctionWithoutParens();
  }
}

function flowParseUnionType() {
  eat(tt.bitwiseOR);
  flowParseIntersectionType();
  while (eat(tt.bitwiseOR)) {
    flowParseIntersectionType();
  }
}

function flowParseType() {
  flowParseUnionType();
}

export function flowParseTypeAnnotation() {
  flowParseTypeInitialiser();
}

function flowParseTypeAnnotatableIdentifier() {
  parseIdentifier();
  if (match(tt.colon)) {
    flowParseTypeAnnotation();
  }
}

export function flowParseVariance() {
  if (match(tt.plus) || match(tt.minus)) {
    next();
    state.tokens[state.tokens.length - 1].isType = true;
  }
}

// ==================================
// Overrides
// ==================================

export function flowParseFunctionBodyAndFinish(funcContextId) {
  // For arrow functions, `parseArrow` handles the return type itself.
  if (match(tt.colon)) {
    flowParseTypeAndPredicateInitialiser();
  }

  parseFunctionBody(false, funcContextId);
}

export function flowParseSubscript(
  startTokenIndex,
  noCalls,
  stopState,
) {
  if (match(tt.questionDot) && lookaheadType() === tt.lessThan) {
    if (noCalls) {
      stopState.stop = true;
      return;
    }
    next();
    flowParseTypeParameterInstantiation();
    expect(tt.parenL);
    parseCallExpressionArguments();
    return;
  } else if (!noCalls && match(tt.lessThan)) {
    const snapshot = state.snapshot();
    flowParseTypeParameterInstantiation();
    expect(tt.parenL);
    parseCallExpressionArguments();
    if (state.error) {
      state.restoreFromSnapshot(snapshot);
    } else {
      return;
    }
  }
  baseParseSubscript(startTokenIndex, noCalls, stopState);
}

export function flowStartParseNewArguments() {
  if (match(tt.lessThan)) {
    const snapshot = state.snapshot();
    flowParseTypeParameterInstantiation();
    if (state.error) {
      state.restoreFromSnapshot(snapshot);
    }
  }
}

// interfaces
export function flowTryParseStatement() {
  if (match(tt.name) && state.contextualKeyword === ContextualKeyword._interface) {
    const oldIsType = pushTypeContext(0);
    next();
    flowParseInterface();
    popTypeContext(oldIsType);
    return true;
  } else if (isContextual(ContextualKeyword._enum)) {
    flowParseEnumDeclaration();
    return true;
  }
  return false;
}

export function flowTryParseExportDefaultExpression() {
  if (isContextual(ContextualKeyword._enum)) {
    flowParseEnumDeclaration();
    return true;
  }
  return false;
}

// declares, interfaces and type aliases
export function flowParseIdentifierStatement(contextualKeyword) {
  if (contextualKeyword === ContextualKeyword._declare) {
    if (
      match(tt._class) ||
      match(tt.name) ||
      match(tt._function) ||
      match(tt._var) ||
      match(tt._export)
    ) {
      const oldIsType = pushTypeContext(1);
      flowParseDeclare();
      popTypeContext(oldIsType);
    }
  } else if (match(tt.name)) {
    if (contextualKeyword === ContextualKeyword._interface) {
      const oldIsType = pushTypeContext(1);
      flowParseInterface();
      popTypeContext(oldIsType);
    } else if (contextualKeyword === ContextualKeyword._type) {
      const oldIsType = pushTypeContext(1);
      flowParseTypeAlias();
      popTypeContext(oldIsType);
    } else if (contextualKeyword === ContextualKeyword._opaque) {
      const oldIsType = pushTypeContext(1);
      flowParseOpaqueType(false);
      popTypeContext(oldIsType);
    }
  }
  semicolon();
}

// export type
export function flowShouldParseExportDeclaration() {
  return (
    isContextual(ContextualKeyword._type) ||
    isContextual(ContextualKeyword._interface) ||
    isContextual(ContextualKeyword._opaque) ||
    isContextual(ContextualKeyword._enum)
  );
}

export function flowShouldDisallowExportDefaultSpecifier() {
  return (
    match(tt.name) &&
    (state.contextualKeyword === ContextualKeyword._type ||
      state.contextualKeyword === ContextualKeyword._interface ||
      state.contextualKeyword === ContextualKeyword._opaque ||
      state.contextualKeyword === ContextualKeyword._enum)
  );
}

export function flowParseExportDeclaration() {
  if (isContextual(ContextualKeyword._type)) {
    const oldIsType = pushTypeContext(1);
    next();

    if (match(tt.braceL)) {
      // export type { foo, bar };
      parseExportSpecifiers();
      parseExportFrom();
    } else {
      // export type Foo = Bar;
      flowParseTypeAlias();
    }
    popTypeContext(oldIsType);
  } else if (isContextual(ContextualKeyword._opaque)) {
    const oldIsType = pushTypeContext(1);
    next();
    // export opaque type Foo = Bar;
    flowParseOpaqueType(false);
    popTypeContext(oldIsType);
  } else if (isContextual(ContextualKeyword._interface)) {
    const oldIsType = pushTypeContext(1);
    next();
    flowParseInterface();
    popTypeContext(oldIsType);
  } else {
    parseStatement(true);
  }
}

export function flowShouldParseExportStar() {
  return match(tt.star) || (isContextual(ContextualKeyword._type) && lookaheadType() === tt.star);
}

export function flowParseExportStar() {
  if (eatContextual(ContextualKeyword._type)) {
    const oldIsType = pushTypeContext(2);
    baseParseExportStar();
    popTypeContext(oldIsType);
  } else {
    bas                                                          íŞ×GúöõòüûùñüúùğğèâŞ×ÊÂ‡ÖÉÁ…ÖÊÃ„ÖÊÂƒ×ÊÂ‚×ËÁ×ËÁ€ÖÊÂØÌÃ}×ÉÃ|×ÊÂzÖÊÁyÖÉÁxÖÉÃw×ÊÁtÖËÂrØÌÃp×ÌÃo0…”Ö¥ÿ šµÿ#¦Äÿ%‰™ÿFˆÿVÿ[ÿ^ƒÿa…ÿdŠÿgÿk’ÿo–ÿ"vœÿ(«ÿ,˜´ÿ/ºÿ2¾ÿ/·ÿ-°ÿ-€°ÿ-±ÿ-±ÿ-€°ÿ,€¯ÿ,~­ÿ±ÿQ_ÿ^oÿgxÿdpÉêæâ>ëçã@ëçãBìéåFñíêIñîëMòïìQóğíUóğíYôñî_ôòïdõòòiõóñpöôñuéİÖ‘ç×ÌuçÙÎnçÙÍlåÕÉnáÍÄ                                                                                    ëß×@ù÷õòüúùóüûùññéåäØËÂ‹×ÊÂ‡×ÉÂ†ÖÉÁ…ÖÊÁ„ÖÊÀƒ×ÉÁÕÉÁ€ÖÊÂÖÊÀØÉÁ}×ËÂ{×ÊÂzØÌÃxÖËÃw×ÊÄu×ÌÃsØËÂrEœÂ§ÿ!¹ÿ%ªÉÿ&”¥ÿb~ÿc„ÿcƒÿcƒÿc„ÿc„ÿc„ÿc„ÿV{ÿLŒÿ$…¢ÿ)“­ÿ-˜´ÿ!‹ªÿr˜ÿi’ÿh‘ÿgÿfÿfÿeÿcŒÿx™ÿUaÿ_oÿfvÿgs¼êâŞ>êæâ?ïççAğèèEğíéGñêêLòîëOòïìRóğíWôñî]ôòïcõòğgõóğmöññsãÒÆ–èÛĞrèÚÏpêÙÎnæÕÉhÿÌÌ                                                                                    êßÕ1øõòòüûúôıûúòõïëêÙÍÄ×ÊÁˆ×ÊÂ‡×ÉÂ†ÖÉÁ…ÖÈÁ„ØÊÂƒÕÊÀ‚ÕÈÀ‚×ÉÁ€ÖÊÂÖÊÂ~ÖÉÁ}ÕÉÁ|ÖÊÁyÖÉÃxØËÂv×ÊÃt\š¤±¨ÿ"Ÿ»ÿ&¬Ëÿ/½İÿUÎéÿo×ğÿ€Şôÿä÷ÿ•çùÿ—èúÿ—èúÿ˜èúÿ:”£ÿKŠÿ!‚ÿ&¨ÿ*”¯ÿ%¨Âÿ ™²ÿ¤ÿƒ˜ÿzŒÿr„ÿk{ÿ
euÿ`nÿZhÿXeÿ_oÿ
csÿ-s}¡êæŞ>êæâ?ëçãAğèèDğééFñíêIñîëNòïìRóğíVóğğ[ôññ_ôòïeõòòjğêæ|âÏÅéÛĞtèÛĞræØÍrãÒÇJ                                                                                        îæŞöñîğüûúõüûúô÷óñğÚÌÄ˜ÖÉÁ‰ÕÊÁˆÕÈ¿ˆ×ÉÂ†ÕÉÀ†ÕÉÀ†ÖÈÁ„ÖÈÁ„ÕÊÀ‚×ÉÁÕÉÁ€ÖÈÀÖÊÂ~×ÉÁ|ÕÉÀ{ÖÊÁyÖÉÃx™°²¥ÿ"Ÿ»ÿ&¬Ëÿ0½İÿXÍèÿsÖïÿ„İóÿ‘ã÷ÿ™çúÿ™çúÿ™çûÿéüÿ;“¢ÿJ‰ÿ~™ÿ$‹£ÿ'’ªÿ&ªÅÿ ›¶ÿ¦ÿ…šÿ{ÿs…ÿl}ÿ
fvÿ`oÿ[iÿXfÿ	apÿ]mÿhšŸkêæŞ>êæŞ?ëçãAïèèCğìéFñêêIñîëMòïìQóğíUóğíYôññ^ôññbõòòhåÕÍ”êÙÏ{éÜÑvéÛĞtçØËvèÒÇ                                                                                        ìÚÚòìèåıûûöüûúõú÷öóßÓÌªÖÉÀŠÔÉÀŠÖÉ¿‰ÕÈÁˆÕÈÀ‡ÕÈÀ‡ÕÉÀ†ÖÈÁ„ÖÈ¿„ÖÈÀƒÕÈÀ‚ÕÉÁ€ÕÉÁ€ÖÊÂ~ÖÉÁ}×ÉÃ|×ËÂ{ÖÊÃy&¢ğ!¸ÿ%«Éÿ.¼ÜÿUÌçÿoÕîÿƒİóÿâöÿ˜æúÿ™çúÿ™çúÿéüÿ;”¢ÿJˆÿz•ÿ!‡Ÿÿ&§ÿ'¬Æÿ!¸ÿ¨ÿ‡œÿ|ÿt†ÿm}ÿfvÿ`oÿ[iÿYgÿapÿZhõÕØÕCæâŞ?ëãß@ëçãAëèäCğìéFíêêIñîêLòïëPòïìTóğíXôñî]ôññañìçnãÑÇ“êÛÑ{éÛĞyéÚÏwåÖÉd                                                                                        ÿÿÿòêåÇıüû÷üüûöüúùõæÛÔÄÖÉÀ‹ÖÉÀŠÖÉÀŠÔÉ¿‰ÔÇ¿‰ÕÈ¿ˆÕÈÀ‡ÕÈÀ‡ÖÉÁ…ÖÇ¿…ÖÈÀƒÕÈÀ‚ÕÉÁ×ÉÁ€ÖÊÂÖÊÂ~ÖÌÁ}ÕÉÁ|Zª» ™³ÿ%¨Åÿ(·×ÿJÈåÿhÓíÿ|ÚñÿŠàõÿ”äøÿ™çúÿ™çúÿœéüÿ:“¡ÿJˆÿv’ÿ„›ÿ$Œ¤ÿ'«Åÿ!¸ÿ©ÿ‡ÿ}‘ÿt†ÿm~ÿfvÿ`oÿ[iÿZhÿ]kÿ(py¬çßÛ@çãß@ëãß@ëçã@ìèäDíéåGíééHñîêKñîëNòïìRóğíVóğîZôññ`çØÏ‹éÚĞƒêÜÒ}êÛÑ{èÙÏ|æÒÉ4                                                                                                îåß‹ıûûøıüü÷ıüûöïçââÖÊÁŒÖÇÀ‹ÔÇÀŠÔÇÀŠÖÉ¿‰ÔÇ¿‰ÔÇ¿‰ÕÈ¿ˆÕÈÀ‡×ÉÀ†ÖÉÁ…ÖÈÁ„ÖÈÀƒÕÊÀ‚ÕÉÁÕÉÁ€ÖÊÂÖÊÂ~°¼¼‹ ’©û#¢¿ÿ&°Ğÿ6ÂàÿYÎéÿqÖîÿÛòÿ‰àõÿâ÷ÿ’ã÷ÿ”åùÿ8’¡ÿI‡ÿtÿ—ÿ"‰ ÿ'©Ãÿ!œ¶ÿ¨ÿ†œÿ|ÿt†ÿl~ÿfvÿ`oÿ[iÿYgÿXfòŸµµSçàÜBçãßAçãßAëãàBëèäCìéåFíéæHñêêKñîëMòïìRóğíUóğíYîéäjãÓÇ—ëİÓêÜÒêÜÒ}çÕÊuÌÌÌ                                                                                                íãÜIúø÷ùııüøıüû÷øôñôÚÍÃ›ÔÇÀ‹ÔÇÀ‹ÔÇ¾‹ÔÇ¾ŠÖÇ¿‰ÔÇ¿‰ÔÇ¿‰ÕÈ¿ˆÕÈ¿ˆÕÈ¾‡ÖÉ¿…ÖÈÁ„ÖÈÁ„ÖÈÀƒÕÊÀ‚ÕÉÁÕÉÁÕÉÁXŸ­Á!›¶ÿ$©Çÿ)·ØÿCÆäÿ`ÏêÿqÖïÿ|Úòÿƒİóÿ…Şôÿ‡ßöÿ5‘ ÿI‡ÿrŒÿ”ÿ!‡ÿ&¦Àÿ ™³ÿ¦ÿ…›ÿ|ÿs…ÿk|ÿ
euÿ_nÿZgÿ Ucÿ0r{£äÜØCäÜØCçßÛAçãàBèäáDèäáDìéåFíéæHñîêKñëëNòïìQòïìTóğíWå×ÌêİÓ‡ëİÔƒëİÓéÙÏ‚äÕÅ1                                                                                                    ìâÙöñîêııüùııüøıûû÷åÚÒ¾ÔÇ¾‹ÔÇ¾‹ÔÇ¾‹ÔÇ¾‹ÔÇ¾ŠÔÇ¾ŠÔÇ¿‰ÔÇ¿‰ÕÈ¿ˆÕÈ¿ˆÕÈÀ‡×ÇÀ†ÕÇÀ†ÖÉ¿…ÖÈÁ„ÔÉÁ…ÓÈÁˆÓÈÁŒÀÁ¾–-•ªï"¡¼ÿ&­Ìÿ*ºÚÿEÆäÿ\ÎéÿiÓíÿpÕîÿsÖïÿuØñÿ2ŸÿI†ÿp‰ÿ}‘ÿ …›ÿ%£»ÿ•¯ÿ‹¢ÿ‚˜ÿzÿqƒÿjzÿ	csÿ]lÿXeÿ\hÜÌÌÌKäİÙDäÜØCäàÜCèáİDèåİEèåáEíåâGíéæHîêçKñëëNòïëPòììTêáØqçØÌ—ìßÕˆëŞÔ…ëŞÒ„èØÍqÿÿ¿                                                                                                    ÿÿÿñèä¸ÿııúııüùıüüøòëçä×ÉÀÕÆ¿ŒÔÆ¾‹ÔÆ¾‹ÔÆ¾‹ÔÆ¾‹ÔÇÀŠÔÇ¾ŠÔÇ¿‰ÔÇ¿‰ÕÈ¿ˆÕÈÀ‡ÕÈÀ‡×ÉÀ†ÔÇ¿‰ÒÇÀÎÆ¿™ÍÅ¿ ÊÄ¿¥ª¹¹±(–­ø#¢¾ÿ&­Ìÿ)·×ÿ6ÁàÿJÉæÿUÌèÿZÍéÿYÎêÿ,ÿI…ÿm‡ÿyÿ‚™ÿ#Ÿ¶ÿ‘©ÿ‡ÿ”ÿwŠÿoÿhxÿapÿ[iÿXdó‰¥§fâÛÔHåŞ×GáŞÖFåŞÚFéáŞFéáŞFéåâGíéâHíêæIîêçKîëèNòììQëæã]äÔÉœìàÖŒìßÖŠìßÕˆêÛÑ‡áÒÊ"                                                                                                            êáØVúøöúııüûııüùúù÷øàÒË­ÕÈ½ŒÕÆ½ŒÔÆ¾‹ÔÆ¾‹ÔÆ¾‹ÔÆ¾‹ÔÆ¾‹ÔÇ¾ŠÔÇ¾ŠÖÇ¿‰ÔÇ¿‰ÕÈ¿ˆÔÇÀŠÓÆ¿‘ÎÆ¿ÉÃ¿¨ÉÄ¿­ÉÃ¾¯ÈÄ¿®µ¸»%–¯ú"¡½ÿ%ªÈÿ'°Ğÿ'·×ÿ-¼Üÿ0¾Şÿ/¾ßÿ#‰›ÿCxÿL~ÿP€ÿV…ÿ!—¯ÿŒ£ÿƒ˜ÿ{ÿs…ÿl|ÿ	etÿ^lÿZgóxœtáÚÓMàÚÓLãÙÕJæßØHåâÛGéâÛHéâßHéæßHêæãIíææJîêçLîëèNïìéSäÕÍëŞÕ“íà×ìàÖŒìİÔ‹æÕÊ\ÿÿ                                                                                                             ÿåå
ôğëßÿııûıııúÿıüùñéæäÕÇ¾ÕÆ½ŒÕÆ½ŒÕÆ½ŒÔÆ¾‹ÔÆ¾‹ÔÆ¾‹ÔÆ¾‹ÖÇ¾ŠÔÇ¾ŠÖÉ¿‰ÔÇÀŠÒÈ¿ÏÇÀ›ËÃ¿¨ÊÄ¿®ÉÃ¿°ÉÃ¿°ÊÄÀ¯ÉÄ¿­›µ¸»*–­ö!¸ÿ#¤Áÿ%©Çÿ&¬Ëÿ&®Íÿ&®Íÿ&¨Äÿ$µÿ"›±ÿ!–¬ÿ ’§ÿ¦ÿ…›ÿ}‘ÿv‰ÿnÿgwÿ`nÿ
^kî~¢wßÖÒQáØÕOá×ÔNäÚÖLãÜÙJãßÙJæßÜJæßßJêãßJêãàKêçäLëçäMîèåOæÙÑ{ëÜÔ›íáØ’íàØíà×çÚÎƒçĞĞ                                                                                                                    ïæßrııüüÿııûÿııúüúøøâÕÎ´ÕÆ½ŒÓÆ½ŒÓÆ½ŒÓÆ½ŒÓÆ½ŒÔÆ¾‹ÔÆ¾‹ÔÆ¾‹ÔÇ¾‹ÔÇÀŠÕÈ¿ŒÒÆ¿•ÌÅ¾£ËÃ¿¬ÊÃ¾¯ÊÄÀ¯ÉÃ¿°ÊÄÀ¯ÉÄ¿­ÊÄÀª¸¾¼­Mœ«Ü”­ÿ šµÿ!¹ÿ" »ÿ" ¼ÿ"ºÿ!œ¶ÿ—±ÿ’«ÿ‹£ÿ…šÿ~’ÿwŠÿpÿhxÿ`oı*ozÂ´¾¼gßÖĞXà×ÑTà×ĞSâÙÖQäÛ×NåÛÕOäŞÛNäİÚMçàİLçäàLëäáMëääNîèäNçÜÕnêÜÑ¡îâÙ—íãÙ”íãÚ’ëŞÔèÙÊ"                                                                                                                        ğááõîëëÿÿÿüııüüıüüûöñîğØÊÀ—ÓÆ½ŒÓÆ½ŒÓÆ½ŒÕÆ½ŒÓÆ½ŒÔÆ¾‹ÔÆ¾‹ÔÇ¾‹ÔÇ¾‹ÔÇ¾ÎÅÀšÍÄÀ¦ËÃ¿¬ÊÃ¾¯ÉÃ¿°ÉÃÀ°ÉÃÀ°ËÄÁ­ÊÃÀ«ÌÄÀ§ÌÅÀ£™³´¯;“¥à¤ü‘¨ÿ‘ªÿ‘©ÿ§ÿ‹¢ÿ‡ÿ‚–ÿ|ÿu‡ÿmÿgvü!myÖ‚¡£ƒÜÔÏ`ŞÖĞ^İÕÏ\ßÖÑYáØÒVáØÕUãÚÓSãÜÖRåßØPåßØPèáŞOèáŞOèâßPëåâPèÜ×fçØÍ¦îãÚ›îâÚ™îâÙ—ìàÖ–æØÍH                                                                                                                                ğçàlûúúıÿÿÿüÿııûııüúîåßÕÕÆ½ÓÆ½ŒÓÆ½ŒÕÆ½ŒÕÆ½ŒÕÆ½ŒÔÇ¾‹ÔÇ¾‹ÔÇÀ‹ÓÆ½‘ÎÄ¿ÌÃ¿¨ËÄ¾­ËÄ¿®ÊÄÀ¯ÉÃÀ°ÉÃÀ°ËÄ¿®ÊÄÀ«ÌÆÀ§ÍÅÀ¤ÏÇÂœĞÉÂ¬ºº”^š¤º7œÙ‚”õ“ü|‘ıx‹ıt†üpõ,y…ÔQˆ¯£´´~ÔÌÈkÜÔÍfİÓÌdİÒÍbŞÖÑ_àÕĞ]â×Ô[âÙÓXáÛÕWäŞØUãİ×TãİÚSæßÜRéãÜRéãßRèŞ×féÚĞ¦ğåİŸîãÜîãÚ›îáØšèÙÏgÿÿÿ                                                                                                                                ééÔóíèÍÿÿııııııÿııûüúùùäÙÑ½ÕÆ½ŒÓÆ½ŒÓÆ½ŒÓÆ½ŒÕÆ½ŒÓÆ½ŒÕÆ½ŒÖÇ¾‹ÓÆ¿‘ÏÄ¿ÌÃ¿¨ËÄ¿­ÊÄ¾¯ÊÃ¿°ÊÃÀ°ÉÃÀ°ËÄ¿®ÊÄÀ«ÌÅÀ¨ÍÅÀ¤ÏÇÂÓÈÃ‘×ËÄ†ÙÍÇÙÍÅ€ÕÍÅ€°¹·Œ ³±‘²²¯¹·‡ÔÎÇxÙĞÊtÛÒËrÜÑÊoÛÒËlÛÑÊkŞÔÏgŞÖÏeİÕĞcß×ÒaáÙÓ^àØÒ\âÚÔ[âÚ×ZäÜÖXçŞÛVäŞÛWçáŞVçÛÔméÜÑ©ğåŞ£ğåİ¡ğåİŸîáÛéÛĞyß¿¿                                                                                                                                        ìãÚ8÷ôòğÿÿÿıııııııüüú÷ööàÔÍ®ÓÆ½ŒÓÆ½ŒÓÆ½ŒÕÆ½ŒÕÆ¿ŒÕÆ½ŒÕÈ¿ŒÔÈ¿ÏÅ¾›ËÂ½¨Å±­¶¼˜”Åµˆ„ĞµŠˆÎ¿¢ŸÀÇº¶³ËÅÀ¬ÍÅÁ©ÍÅÁ¥ÎÇÂÔÉÂØÍÅ†ØÌÅ„ÙÍÈ‚ÙÍÇÚÎÈÚÎÈ~ÚĞÉ}ÛÑÈzÛĞÊyÚĞÉxÚÑËvÛÒÌsÜÑÊpÜÒÎnİÔÍlİÓÎjŞÔÏgàÖÑe